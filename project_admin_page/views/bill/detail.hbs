<div class="row">
    <div class="col-md-12 mb-2">
        <div class="page-header-breadcrumb">
            <ul class="breadcrumb-title">
                <li class="breadcrumb-item"><a th:href="@{/admin}"> <i class="feather icon-home"></i> Trang chủ
                    </a></li>
                <li class="breadcrumb-item"><a href="#!">Danh sách chi tiết hóa đơn</a>
                </li>
            </ul>
        </div>
    </div>
    <div class="col-sm-12">
        <div class="card px-3">
            <div class="card-header px-0 pb-2">
                <h2 class="text-uppercase text-center">Danh sách chi tiết hóa đơn</h2>
                <a th:href="@{/admin/detail/add}" class="btn btn-sm btn-primary">Thêm
                    mới</a>
            </div>
            <div class="card-block table-border-style">
                <div class="table-responsive">
                    <table class="table table-striped table-bordered" id="dtBasicExample">
                        <thead>
                            <tr>
                                <th>STT</th>
                                <!-- <th>Id</th> -->
                                <th>Bill id</th>
                                <th>Sản phẩm</th>
                                <th>Số lượng</th>
                                <th>Giá Bán</th>
                                <!-- <th>Hành Động</th> -->
                            </tr>
                        </thead>
                        <tbody>

                            {{#each details}}
                            <tr>
                                <td>{{@key}}</td>
                                <td>{{bill}}</td>
                                <td>{{product.name}}</td>
                                <td>{{quantity}}</td>
                                <td>{{product.price}}</td>
                                <!-- <td>
											<a 
												th:href="@{/admin/detail/edit/{id}(id=${item.id})}" 
												class="btn btn-sm btn-info btn-round py-1 font-weight-bold">
												Sửa
											</a>
											<a 
												th:href="@{/admin/detail/delete/{id}(id=${item.id})}" 
												class="btn btn-sm btn-danger btn-round py-1 font-weight-bold">
												Xóa
											</a>
										</td> -->
                            </tr>
                            {{/each}}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>